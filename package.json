{
  "name": "ernie-bot-js",
  "version": "0.5.1",
  "description": "JS library for the ERNIE Bot API",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --outDir dist/"
  },
  "author": "MBearo",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
